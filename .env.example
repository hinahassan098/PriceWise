# Backend (Render API service)
ADMIN_API_KEY=replace-with-a-long-random-secret

# Frontend (Render web service) — same value as backend
ADMIN_API_KEY=replace-with-a-long-random-secret
NEXT_PUBLIC_API_URL=https://pricewise-api-dauj.onrender.com
